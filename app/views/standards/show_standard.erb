<!DOCTYPE html>
  <html>
  <header>
    <link href="<%= url('/style.css') %>" rel="stylesheet" type="text/css" />
  </header>
  <body>
    <b><%= "Standard Title:" %></b><br><ul><li><%= @standard.title %></li></ul><br>
    <b><%= "Standard Description:" %></b><br><ul><li><%= @standard.description %></li></ul><br>
    <b><%= "Add Resources:" %></b><br>
    <ul>
    <% @standard.resources.each do |resource| %>
        <ul><b><%= resource.title  %></b>
          <section id="resource_link">
            <a href="<%= resource.link %>" target="_blank"><%= resource.link %></a>
          </section>
          </ul><br>
    <% end %>
    </ul>

    <a href='/standards/<%= @standard.id %>/edit' class="spacious primary">Edit Standard</a><br><br>
    <a href='/users/homepage' class="spacious primary">Return to Homepage</a><br><br>

    <form action='/logout' method='POST'>
        <button type="submit">Log Out</button>
  </body>
</html>
